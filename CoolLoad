local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RE = ReplicatedStorage:WaitForChild("RE")

local NameTextEvent = RE:WaitForChild("1RPNam1eTex1t")
local NameColorEvent = RE:WaitForChild("1RPNam1eColo1r")

-- FunÃƒÂ§ÃƒÂ£o para disparar eventos com seguranÃƒÂ§a 
local function fireEvent(event, ...)
    if event then
        event:FireServer(...)
    else
        warn("Evento nÃƒÂ£o encontrado:", event)
    end
end

-- Nome de RolePlay
fireEvent(NameTextEvent, "RolePlayName", "Xavier Hub by Ricky")

-- Cor do Nome (preto)
fireEvent(NameColorEvent, "PickingRPNameColor", Color3.new(255, 140, 0))

-- Bio de RolePlay
fireEvent(NameTextEvent, "RolePlayBio", "🇮🇩 made in Indonesia 🇮🇩")

-- Cor da Bio (vermelho)
fireEvent(NameColorEvent, "PickingRPBioColor", Color3.new(102, 51, 153))

-- Xavier Hub | Fake Loading Script
-- Asset ID: 107795841666423

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local lp = Players.LocalPlayer

-- ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "XavierHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 9999
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = CoreGui

-- Semi-transparent black overlay
local Overlay = Instance.new("Frame")
Overlay.Name = "Overlay"
Overlay.Size = UDim2.fromScale(1, 1)
Overlay.Position = UDim2.fromScale(0, 0)
Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Overlay.BackgroundTransparency = 0.45
Overlay.BorderSizePixel = 0
Overlay.ZIndex = 1
Overlay.Parent = ScreenGui

-- Blur effect on Lighting
local Blur = Instance.new("BlurEffect")
Blur.Size = 16
Blur.Parent = game.Lighting

-- Main card (centered)
local Card = Instance.new("Frame")
Card.Name = "Card"
Card.Size = UDim2.fromOffset(340, 160)
Card.AnchorPoint = Vector2.new(0.5, 0.5)
Card.Position = UDim2.fromScale(0.5, 0.5)
Card.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
Card.BackgroundTransparency = 0.08
Card.BorderSizePixel = 0
Card.ZIndex = 2
Card.Parent = ScreenGui

Instance.new("UICorner", Card).CornerRadius = UDim.new(0, 14)
Instance.new("UIStroke", Card).Color = Color3.fromRGB(60, 80, 120)

-- Roblox asset logo (left side)
local Logo = Instance.new("ImageLabel")
Logo.Name = "Logo"
Logo.Size = UDim2.fromOffset(38, 38)
Logo.Position = UDim2.fromOffset(22, 30)
Logo.BackgroundTransparency = 1
Logo.Image = "rbxassetid://107795841666423"
Logo.ImageColor3 = Color3.fromRGB(255, 255, 255)
Logo.ImageTransparency = 0
Logo.ScaleType = Enum.ScaleType.Fit
Logo.ZIndex = 3
Logo.Parent = Card

-- "Xavier Hub" title
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.fromOffset(240, 38)
Title.Position = UDim2.fromOffset(70, 26)
Title.BackgroundTransparency = 1
Title.Text = "Xavier Hub"
Title.TextColor3 = Color3.fromRGB(220, 230, 255)
Title.TextSize = 22
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 3
Title.Parent = Card

-- Status sub-label
local Sub = Instance.new("TextLabel")
Sub.Name = "Sub"
Sub.Size = UDim2.fromOffset(296, 18)
Sub.Position = UDim2.fromOffset(22, 68)
Sub.BackgroundTransparency = 1
Sub.Text = "Loading, please wait..."
Sub.TextColor3 = Color3.fromRGB(130, 150, 200)
Sub.TextSize = 13
Sub.Font = Enum.Font.Gotham
Sub.TextXAlignment = Enum.TextXAlignment.Left
Sub.ZIndex = 3
Sub.Parent = Card

-- Progress bar background track
local BarBg = Instance.new("Frame")
BarBg.Size = UDim2.fromOffset(296, 8)
BarBg.Position = UDim2.fromOffset(22, 108)
BarBg.BackgroundColor3 = Color3.fromRGB(40, 45, 65)
BarBg.BorderSizePixel = 0
BarBg.ZIndex = 3
BarBg.Parent = Card
Instance.new("UICorner", BarBg).CornerRadius = UDim.new(1, 0)

-- Progress bar fill (light blue)
local BarFill = Instance.new("Frame")
BarFill.Name = "BarFill"
BarFill.Size = UDim2.fromScale(0, 1)
BarFill.BackgroundColor3 = Color3.fromRGB(80, 170, 245)
BarFill.BorderSizePixel = 0
BarFill.ZIndex = 4
BarFill.Parent = BarBg
Instance.new("UICorner", BarFill).CornerRadius = UDim.new(1, 0)

-- Percentage label
local PctLabel = Instance.new("TextLabel")
PctLabel.Size = UDim2.fromOffset(296, 20)
PctLabel.Position = UDim2.fromOffset(22, 122)
PctLabel.BackgroundTransparency = 1
PctLabel.Text = "0%"
PctLabel.TextColor3 = Color3.fromRGB(100, 160, 220)
PctLabel.TextSize = 11
PctLabel.Font = Enum.Font.Gotham
PctLabel.TextXAlignment = Enum.TextXAlignment.Right
PctLabel.ZIndex = 5
PctLabel.Parent = Card

-- Loading animation (4 second duration)
local duration = 4
local steps = 80
local interval = duration / steps

local statusList = {
	[10] = "Initializing modules...",
	[30] = "Loading assets...",
	[50] = "Connecting to server...",
	[70] = "Verifying permissions...",
	[90] = "Almost done...",
}

for i = 1, steps do
	task.wait(interval)
	local pct = i / steps
	BarFill.Size = UDim2.fromScale(pct, 1)
	PctLabel.Text = math.floor(pct * 100) .. "%"
	local key = math.floor(pct * 100)
	if statusList[key] then
		Sub.Text = statusList[key]
	end
end

-- Finished: clean up, then execute loadstring
Sub.Text = "Done! Launching..."
task.wait(0.4)

Blur:Destroy()
ScreenGui:Destroy()

-- Replace the URL below with your raw script link
loadstring(game:HttpGet("https://raw.githubusercontent.com/mousegay570-source/potential-fiesta/refs/heads/main/NoTezt"))()
